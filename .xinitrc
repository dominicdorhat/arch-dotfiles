# ~/.xintirc

# NOTE: 
## .xintrc is only used by X when a xorg-server session is start via startx (which runs intix)

# i3-gaps setup
~/.fehbg &
compton &
~/scripts/input-devices/toggle-touchpad.sh &
exec i3 

